<!DOCTYPE html>
<html>
<head>
  <title>WordScrambleApp</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
	<header>
			<ul id="navbar">
		<% if session[:current_user_id] %>
			<%= render partial: 'sessions/navbar', locals: { navbar: [:welcome, :profile, :home] } %>
		<% else %>
			<%= render partial: 'sessions/navbar', locals: { navbar: [:log_in, :sign_up, :home] } %>
		<% end %>
		| <%= link_to "Log Out", sessions_path, method: :delete %>
	</ul>
	</header>

	<h1 class="header"> Word Scramble</h1>

<button class="start">Start Game</button>

<%= yield %>
<br><br>
<div class="timer">
	<p class="timer"> </p>
</div>

<div class="score">
	<p class="score">Score: 0</p>
</div>

<button class="submit-word">Submit Word</button>

<div class="boggle-board">
</div>
<div class="word-list">
</div>


</body>
</html>
